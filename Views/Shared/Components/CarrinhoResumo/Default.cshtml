@model int 

<a class="nav-link text-dark" asp-area="" asp-controller="Carrinho" asp-action="Index">
    🛒 Carrinho 
        <span id="cart-count" class="badge bg-danger @(Model == 0 ? "d-none" : "")">@Model</span>
</a>